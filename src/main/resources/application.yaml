server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JonathanLopez0327/budget-app-config
          privateKey: nini
          clone-on-start: true
